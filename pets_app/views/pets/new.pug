extends base

block content
  h3 Give #{owner.name} a new pet!
  form(action=`/owners/${owner.id}/pets` method="POST")
    label Name: 
    input(type="text" name="name")
    label Kind of pet:
    input(type="text" name="species")
    input(type="submit" value="Submit")